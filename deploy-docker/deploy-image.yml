---
- name: Pull docker image
  hosts: all
  tasks:
    - name: Pull mini-project image
      docker_image:
        name: shmfaraz15/mini-project
        source: pull

    - name: starting docker container
      command: docker run -it --name user3 shmfaraz15/mini-project:latest /bin/bash

#    - name: Execute commands in docker container
#      command: docker exec -it my_container bash -c 'echo "Hello remote machine"'
